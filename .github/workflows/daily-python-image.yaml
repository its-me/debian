name: Daily python image

on:
  schedule:
    - cron: "45 9 * * *"

jobs:
  build:
    name: Build and push to GHCR
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      hash: ${{ steps.exist.outputs.hash }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile.python
          load: true
          tags: debian-python

      - name: Check if image exists in GHCR
        id: exist
        run: |
          hash=$(docker run --rm debian-python sh -c 'dpkg -l && python3 --version' | sha256sum | cut -d' ' -f1)
          echo "hash=$hash" >> $GITHUB_OUTPUT
          if docker manifest inspect ghcr.io/${{ github.repository }}:python-$hash > /dev/null 2>&1; then
            echo "exist=true" >> $GITHUB_OUTPUT
          else
            echo "exist=false" >> $GITHUB_OUTPUT
          fi

      - name: Push image to GHCR
        run: |
          date_tag=$(date -u +%y%m%d)
          if [ "${{ steps.exist.outputs.exist }}" != "true" ]; then
            docker tag debian-python ghcr.io/${{ github.repository }}:python-${{ steps.exist.outputs.hash }}
            docker push ghcr.io/${{ github.repository }}:python-${{ steps.exist.outputs.hash }}
          fi
          docker buildx imagetools create \
            --tag ghcr.io/${{ github.repository }}:python-$date_tag \
            --tag ghcr.io/${{ github.repository }}:python \
            ghcr.io/${{ github.repository }}:python-${{ steps.exist.outputs.hash }}

  push-hub:
    name: Publish to Docker Hub
    needs: build
    runs-on: ubuntu-latest
    permissions:
      packages: read
    env:
      REPOSITORY: debian
    steps:
      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.HUB_USERNAME }}
          password: ${{ secrets.HUB_TOKEN }}

      - name: Check if image exists in Docker Hub
        id: exist
        run: |
          if docker manifest inspect ${{ secrets.HUB_USERNAME }}/$REPOSITORY:python-${{ needs.build.outputs.hash }} > /dev/null 2>&1; then
            echo "exist=true" >> $GITHUB_OUTPUT
          else
            echo "exist=false" >> $GITHUB_OUTPUT
          fi

      - name: Publish image to Docker Hub
        run: |
          date_tag=$(date -u +%y%m%d)
          if [ "${{ steps.exist.outputs.exist }}" != "true" ]; then
            docker buildx imagetools create \
              --tag ${{ secrets.HUB_USERNAME }}/$REPOSITORY:python-${{ needs.build.outputs.hash }} \
              ghcr.io/${{ github.repository }}:python-${{ needs.build.outputs.hash }}
          fi
          docker buildx imagetools create \
            --tag ${{ secrets.HUB_USERNAME }}/$REPOSITORY:python-$date_tag \
            --tag ${{ secrets.HUB_USERNAME }}/$REPOSITORY:python \
            ghcr.io/${{ github.repository }}:python-${{ needs.build.outputs.hash }}

  push-quay:
    name: Publish to Quay
    needs: build
    runs-on: ubuntu-latest
    permissions:
      packages: read
    env:
      REPOSITORY: debian
    steps:
      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to Quay
        uses: docker/login-action@v4
        with:
          registry: quay.io
          username: ${{ secrets.QUAY_USERNAME }}
          password: ${{ secrets.QUAY_TOKEN }}

      - name: Check if image exists in Quay
        id: exist
        env:
          QUAY_NAMESPACE: ${{ secrets.QUAY_USERNAME }}
        run: |
          if docker manifest inspect quay.io/${QUAY_NAMESPACE%%+*}/$REPOSITORY:python-${{ needs.build.outputs.hash }} > /dev/null 2>&1; then
            echo "exist=true" >> $GITHUB_OUTPUT
          else
            echo "exist=false" >> $GITHUB_OUTPUT
          fi

      - name: Publish image to Quay
        env:
          QUAY_NAMESPACE: ${{ secrets.QUAY_USERNAME }}
        run: |
          date_tag=$(date -u +%y%m%d)
          if [ "${{ steps.exist.outputs.exist }}" != "true" ]; then
            docker buildx imagetools create \
              --tag quay.io/${QUAY_NAMESPACE%%+*}/$REPOSITORY:python-${{ needs.build.outputs.hash }} \
              ghcr.io/${{ github.repository }}:python-${{ needs.build.outputs.hash }}
          fi
          docker buildx imagetools create \
            --tag quay.io/${QUAY_NAMESPACE%%+*}/$REPOSITORY:python-$date_tag \
            --tag quay.io/${QUAY_NAMESPACE%%+*}/$REPOSITORY:python \
            ghcr.io/${{ github.repository }}:python-${{ needs.build.outputs.hash }}
